# import Patient from "./patient.graphql"

type Operator {
  id: ID!
  name: String!
  patient: Patient
}

# Query
type Query {
  operator: [Operator!]!
}

# Mutation
type Mutation {
  createOperator(name: String!): Operator!
}
