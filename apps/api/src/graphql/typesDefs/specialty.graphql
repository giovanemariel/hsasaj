# import Doctor from "./doctor.graphql"

type Specialty {
  id: ID!
  name: String!
  doctors: [Doctor]
}

# Query
type Query {
  specialty: [Specialty!]!
}

# Mutation
type Mutation {
  createSpecialty(name: String!): Specialty
}
