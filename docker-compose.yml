version: "3.4"

services:
  api:
    container_name: api
    build:
      context: ./apps/api
      dockerfile: Dockerfile

  mysql:
    container_name: mysql
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD:
